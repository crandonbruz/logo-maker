class shapes {
    constructor(text, color, textColor) {
        this.text = text;
        this.textColor = textColor;
        this.color = color;
    }
}

class Square extends Shapes {
    render() {
        return '<svg'
    }
}

